# Git Push Approval Rule

Before staging, committing, or pushing code to any remote git repository (including GitHub):
1. Present a clear summary of all proposed file edits/changes in the chat.
2. Ask the user for explicit review and permission to commit and push.
3. Only execute `git commit` and `git push` once the user has replied with approval.
